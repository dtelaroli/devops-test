version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - yarn
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: frontend/build
    files:
      - '*'
  cache:
    paths:
